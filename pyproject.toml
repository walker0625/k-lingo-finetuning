[project]
name = "k-lingo-finetuning"
version = "0.1.0"
description = "리포지토리 마이그레이션 및 간단한 스크립트 실행용"
readme = "README.md"
# 호환성을 위해 3.13 미만 유지 (uv 에러 방지)
requires-python = ">=3.11, <3.13"

dependencies = [
    "huggingface_hub",  
    "python-dotenv",    
]